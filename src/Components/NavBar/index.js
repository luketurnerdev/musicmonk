import React from "react";
import DesktopNav from "./../DesktopNav";

const NavBar = () => {
  // Keep this component incase we want to change
  // responsive design later; check the screen size here
  return (
      <DesktopNav />
  );
};

export default NavBar;
